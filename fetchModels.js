const fs = require('fs'); const env = fs.readFileSync('.env.local', 'utf8'); const match = env.match(/GEMINI_API_KEY=\"?([^\"\r\n]+)\"?/); if (match) { const key = match[1].trim(); fetch('https://generativelanguage.googleapis.com/v1beta/models?key=' + key).then(res => res.json()).then(data => console.log(data.models ? data.models.map(m => m.name).join('\n') : data)).catch(console.error); } else { console.log('No API key found'); }
