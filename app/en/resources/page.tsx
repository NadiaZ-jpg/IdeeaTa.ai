import { ResourcesHubPage, hubMetadata } from "@/components/ResourcePage";

export const metadata = hubMetadata("en");

export default function ResourcesPage() {
  return <ResourcesHubPage locale="en" />;
}
