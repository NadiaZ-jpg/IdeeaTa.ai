import { ResourcesHubPage, hubMetadata } from "@/components/ResourcePage";

export const metadata = hubMetadata("es");

export default function RecursosPage() {
  return <ResourcesHubPage locale="es" />;
}
